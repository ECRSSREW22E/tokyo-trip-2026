const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const s=require('../lib/social.cjs');
const raw=JSON.parse(fs.readFileSync(path.join(__dirname,'..','fixtures','records.json'),'utf8')),records=raw.map(s.normalizeRecord),deduped=s.dedupe(records);assert.equal(deduped.length,raw.length-1);assert.equal(deduped.filter(x=>x.platform==='Dcard').length,2);assert.equal(deduped.filter(x=>x.platform==='PTT').length,1);assert.equal(deduped.find(x=>x.platform==='Threads').confidence,'SEARCH_INDEX_ONLY');assert.equal(s.publishable(deduped.find(x=>x.platform==='Dcard')),true);assert.equal(s.publishable(deduped.find(x=>x.platform==='PTT')),false);assert.equal(s.confidence(deduped.filter(x=>x.platform==='Threads')),'SEARCH_INDEX_ONLY');console.log('PASS social pipeline: canonical URL, dedup, blocked access, confidence and publish gate');
